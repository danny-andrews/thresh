Write test for cli.js.

Remove monorepo logic.

Stats should passed around as a list, not an object. Currently, we have to do a
bunch of conversions back and forth.

Update eslint config to get import rules.
