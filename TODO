Check for the existence of the running asset value in file database. If it
exists, we don't need to make separate request for the previous asset stats
artifact.

Write test for cli.js.

Use named parameters for error message sprintf.

Stats should passed around as a list, not an object. Currently, we have to do a
bunch of conversions back and forth.

Add test coverage threshold back.
