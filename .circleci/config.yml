version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11.4
    environment:
      - CIRCLE_ARTIFACTS: ./example/dist/artifacts
    steps:
      - checkout
      - run: yarn install
      - run: yarn ci
      - run: yarn build
      - run: ./thresh
      - store_artifacts:
          path: ./example/dist/artifacts
          destination: ./
